server:
  port: 8081
  
spring:
  datasource:
     url: jdbc:mysql://localhost:3308/CitizenDB
     password: admin
     username: root
     driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
     
  application:
    name: CITIZEN-SERVICE